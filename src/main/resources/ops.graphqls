# The Root Query for the application
type Query {
    recentPlayers(count: Int = 100, offset: Int = 0): [Player]!

    allTeams: [Team]!
}

# The Root Mutation for the application
type Mutation {
    writePlayer(id: String!, Nom: String!, Prenom: String!) : Player!
}